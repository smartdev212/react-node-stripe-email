import React from 'react';

const Landing = () => {
    return (
        <div className="container" style={{ textAlign: 'center' }}>
            <h1>
                ReactMail
            </h1>
            Collect feedback.
        </div>
    );
};

export default Landing;